# Copyright 2026 Chronos Ledger Contributors
# Licensed under the Apache License, Version 2.0
#
# Dependabot configuration for automated dependency updates.
# Security alerts are filed as PRs immediately; version updates are weekly.
# Grouped updates reduce PR noise, all non-security bumps arrive as a
# single "chore: bump X dependencies" PR per ecosystem per week.

version: 2

updates:
  # ── Python / uv (pip ecosystem) ───────────────────────────────────────
  - package-ecosystem: "pip"
    directory: "/backend"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "06:00"
      timezone: "Asia/Kolkata"
    open-pull-requests-limit: 5
    groups:
      python-deps:
        patterns: ["*"]
        exclude-patterns:
          # Security fixes must not be grouped, they get their own PRs
          - "PyJWT"
          - "bcrypt"
          - "cryptography"
    labels:
      - "dependencies"
      - "python"
    commit-message:
      prefix: "chore(deps)"
      include: "scope"

  # ── Node.js / npm ─────────────────────────────────────────────────────
  - package-ecosystem: "npm"
    directory: "/frontend"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "06:00"
      timezone: "Asia/Kolkata"
    open-pull-requests-limit: 5
    versioning-strategy: "increase"
    groups:
      react-ecosystem:
        patterns:
          - "react"
          - "react-dom"
          - "@types/react*"
          - "next"
      ui-libs:
        patterns:
          - "framer-motion"
          - "lucide-react"
          - "tailwindcss"
          - "@tailwindcss/*"
          - "postcss"
          - "autoprefixer"
      form-and-validation:
        patterns:
          - "react-hook-form"
          - "@hookform/*"
          - "zod"
      state-and-data:
        patterns:
          - "zustand"
          - "axios"
          - "date-fns"
          - "idb"
      pwa:
        patterns:
          - "@ducanh2912/next-pwa"
          - "workbox-*"
      typescript-toolchain:
        patterns:
          - "typescript"
          - "@types/*"
          - "eslint*"
          - "@eslint*"
    # Held back on purpose. Each of these fails the build or restyles the
    # app, so the upgrade is planned work, not a weekly bump. Minor and patch
    # releases still come through.
    ignore:
      - dependency-name: "typescript"
        update-types: ["version-update:semver-major"]
      - dependency-name: "eslint"
        update-types: ["version-update:semver-major"]
      - dependency-name: "@types/node"
        update-types: ["version-update:semver-major"]
      - dependency-name: "tailwindcss"
        update-types: ["version-update:semver-major"]
      - dependency-name: "tailwind-merge"
        update-types: ["version-update:semver-major"]
      - dependency-name: "framer-motion"
        update-types: ["version-update:semver-major"]
      - dependency-name: "lucide-react"
        update-types: ["version-update:semver-major"]
    labels:
      - "dependencies"
      - "javascript"
    commit-message:
      prefix: "chore(deps)"
      include: "scope"

  # ── Docker / GitHub Actions ────────────────────────────────────────────
  # The Dockerfiles live beside the code they build, not at the root, where
  # this looked and failed every week finding none.
  - package-ecosystem: "docker"
    directories:
      - "/backend"
      - "/frontend"
      - "/nginx"
    schedule:
      interval: "weekly"
      day: "tuesday"
      time: "06:00"
      timezone: "Asia/Kolkata"
    open-pull-requests-limit: 3
    # Node stays on the LTS line the frontend is built and typed against.
    ignore:
      - dependency-name: "node"
        update-types: ["version-update:semver-major"]
    labels:
      - "dependencies"
      - "docker"
    commit-message:
      prefix: "chore(deps)"

  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "tuesday"
      time: "06:00"
      timezone: "Asia/Kolkata"
    open-pull-requests-limit: 3
    labels:
      - "dependencies"
      - "ci"
    commit-message:
      prefix: "chore(ci)"
